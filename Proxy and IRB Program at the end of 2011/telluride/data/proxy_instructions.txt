1. Check the boxes next to present members.
2. Click "run proxies" to calculate proxies
3. If attendance changes, use checkboxes to
update proxies, then click "run proxies"
to update.

(the run command:
java -classpath "C:/telluride/classes;" -Djava.library.path="C:\telluride\lib\win32" org.telluride.proxy.GUI.ProxyGUI